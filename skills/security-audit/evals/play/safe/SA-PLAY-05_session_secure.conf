# application.conf

play.http.secret.key = ${?APPLICATION_SECRET}

# Session cookie only ever travels over HTTPS and is invisible to scripts
play.http.session {
  secure = true
  httpOnly = true
  maxAge = 1h
  sameSite = "strict"
}

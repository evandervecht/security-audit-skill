# application.conf

play.filters.enabled += "play.filters.hosts.AllowedHostsFilter"

# Concrete hosts plus a scoped subdomain wildcard; nothing else is served
play.filters.hosts {
  allowed = ["www.example.com", ".api.example.com", "localhost:9000"]
}

name: Debug
on: [push]
jobs:
  dump:
    runs-on: ubuntu-latest
    steps:
      - run: echo '${{ toJSON(github.event) }}'
      - run: echo '${{ toJSON(matrix) }}'
      - run: echo "$API_KEY"
        env:
          API_KEY: ${{ secrets.API_KEY }}

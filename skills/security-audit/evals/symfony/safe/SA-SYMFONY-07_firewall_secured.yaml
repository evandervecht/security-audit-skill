security:
    firewalls:
        main:
            lazy: true
            provider: app_user_provider
            form_login:
                login_path: app_login
    access_control:
        - { path: ^/admin, roles: ROLE_ADMIN }
        # anonymous users are redirected to login, security is enabled

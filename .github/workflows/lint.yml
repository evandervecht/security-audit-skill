name: Lint

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  validate:
    name: Skill Validation
    uses: evandervecht/skill-repo-skill/.github/workflows/validate.yml@main
